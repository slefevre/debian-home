

wrait setup // initialize direcotyr with config files

wraith history ./configs/config.yaml // establist baseline

wraith latest configs/config.yaml // scrape site and compare against baseline

wraith spider configs/spider.yaml // spider according to a configuration
